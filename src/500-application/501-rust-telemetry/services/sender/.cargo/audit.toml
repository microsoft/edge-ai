[advisories]
# instant 0.1.13 - unmaintained transitive via notify-types -> notify-debouncer-full
# -> azure_iot_operations_mqtt 0.9.0. Cannot drop without an upstream SDK release.
# rand 0.9.2 - unsound transitive via uuid -> azure_iot_operations_protocol 0.9.0
# and via opentelemetry_sdk 0.29.0. Cannot drop without upstream releases.
ignore = [
  "RUSTSEC-2024-0384",
  "RUSTSEC-2026-0097",
]
