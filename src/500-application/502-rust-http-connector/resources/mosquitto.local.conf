# Sample Mosquitto configuration for local development
# Listens on default MQTT port
listener 1883 0.0.0.0
protocol mqtt

# Enable WebSockets support
listener 9001 0.0.0.0
protocol websockets

# Allow anonymous connections (not for production!)
allow_anonymous true

# Logging
log_type all
log_dest stdout
