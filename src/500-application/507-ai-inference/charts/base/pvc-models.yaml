---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ai-models-pvc
  labels:
    app: ai-edge-inference
    component: ai-inference
    part-of: edge-ai-inference
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: "local-path"  # Use local-path storage class
