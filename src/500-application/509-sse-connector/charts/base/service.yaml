---
apiVersion: v1
kind: Service
metadata:
  name: sse-server
  namespace: azure-iot-operations
  labels:
    app: sse-server
    component: sse-connector
    part-of: edge-ai-leak-detection
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: sse-server
