[package]
name = "custom-provider"
version = "1.0.0"
license = "MIT"
edition = "2021"

[dependencies]
wit-bindgen = "0.22"
wasm_graph_sdk = { version = "=1.1.3", registry = "aio-sdks" }

[lib]
crate-type = ["cdylib"]

[workspace]
